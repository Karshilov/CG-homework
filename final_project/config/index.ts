export type Config = {
    bg: {
        r: number,
        g: number,
        b: number,
        a: number
    }
}

const config: Config = {
    bg: {
        r: 0,
        g: 0,
        b: 0,
        a: 1
    }
}

export default config;

